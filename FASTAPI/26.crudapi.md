# Building a Full CRUD API with In-Memory Storage (FastAPI)

This is usually the **first complete backend project** students build before learning databases.

---

# What Are We Building?

A Student Management API.

Features:

```text
Create Student
Get All Students
Get One Student
Update Student
Delete Student
```

Storage:

```text
Python Dictionary
```

No database yet.

---

# Final API Endpoints

| Method | Endpoint       | Purpose          |
| ------ | -------------- | ---------------- |
| POST   | /students      | Create Student   |
| GET    | /students      | Get All Students |
| GET    | /students/{id} | Get One Student  |
| PUT    | /students/{id} | Update Student   |
| DELETE | /students/{id} | Delete Student   |

---

# Step 1: Create Project

```text
crud_project/
|
├── main.py
```

---

# Step 2: Install FastAPI

```bash
pip install fastapi uvicorn
```

---

# Step 3: Create Basic Application

```python
from fastapi import FastAPI

app = FastAPI()
```

Run:

```bash
uvicorn main:app --reload
```

Visit:

```text
http://127.0.0.1:8000/docs
```

Swagger opens.

---

# Step 4: Create In-Memory Storage

Think of this as a fake database.

```python
students = {}
```

Current state:

```python
{}
```

No students.

---

# Why Dictionary?

Fast lookup.

```python
students[1]
```

gets student with ID 1 instantly.

---

# Step 5: Create Student (POST)

First feature.

Client sends:

```json
{
    "name":"Ravi",
    "age":21,
    "course":"CSE"
}
```

Server stores it.

---

## Code

```python
@app.post("/students")
def create_student(student: dict):

    student_id = len(students) + 1

    student["id"] = student_id

    students[student_id] = student

    return {
        "message":"Student Created",
        "student":student
    }
```

---

# Internal Flow

Initially:

```python
students = {}
```

---

Request:

```json
{
    "name":"Ravi",
    "age":21
}
```

---

After insertion:

```python
{
    1:{
        "id":1,
        "name":"Ravi",
        "age":21
    }
}
```

---

# Test in Swagger

POST

```text
/students
```

Body:

```json
{
  "name":"Ravi",
  "age":21,
  "course":"CSE"
}
```

---

Response:

```json
{
  "message":"Student Created",
  "student":{
      "id":1,
      "name":"Ravi",
      "age":21,
      "course":"CSE"
  }
}
```

---

# Step 6: Get All Students

Requirement:

```text
Show all students
```

---

## Code

```python
@app.get("/students")
def get_students():

    return {
        "students": list(students.values())
    }
```

---

# Why list()?

Dictionary:

```python
{
    1:{},
    2:{}
}
```

---

We only need:

```python
[
   {},
   {}
]
```

Student records.

---

Request:

```http
GET /students
```

---

Response:

```json
{
  "students":[
      {
          "id":1,
          "name":"Ravi"
      }
  ]
}
```

---

# Step 7: Get Single Student

Requirement:

```text
Get Student By ID
```

---

Request:

```http
GET /students/1
```

---

## Code

```python
from fastapi import HTTPException

@app.get("/students/{student_id}")
def get_student(student_id: int):

    if student_id not in students:

        raise HTTPException(
            status_code=404,
            detail="Student Not Found"
        )

    return students[student_id]
```

---

# Internal Flow

Request:

```http
/students/1
```

---

Check:

```python
if 1 not in students
```

False.

Return student.

---

Response:

```json
{
  "id":1,
  "name":"Ravi",
  "age":21
}
```

---

# Student Not Found Case

Request:

```http
/students/100
```

---

Response:

```json
{
  "detail":"Student Not Found"
}
```

Status:

```http
404 Not Found
```

---

# Step 8: Update Student (PUT)

Requirement:

```text
Replace student information
```

---

Current Student

```json
{
  "id":1,
  "name":"Ravi",
  "age":21
}
```

---

Request

```json
{
  "name":"Rahul",
  "age":25
}
```

---

Updated Record

```json
{
  "id":1,
  "name":"Rahul",
  "age":25
}
```

---

## Code

```python
@app.put("/students/{student_id}")
def update_student(
    student_id: int,
    student: dict
):

    if student_id not in students:

        raise HTTPException(
            status_code=404,
            detail="Student Not Found"
        )

    student["id"] = student_id

    students[student_id] = student

    return {
        "message":"Student Updated",
        "student":student
    }
```

---

# Test

Request:

```http
PUT /students/1
```

Body:

```json
{
  "name":"Rahul",
  "age":25,
  "course":"ECE"
}
```

---

Response:

```json
{
  "message":"Student Updated",
  "student":{
      "id":1,
      "name":"Rahul",
      "age":25,
      "course":"ECE"
  }
}
```

---

# Step 9: Delete Student

Requirement:

```text
Remove Student
```

---

Request:

```http
DELETE /students/1
```

---

## Code

```python
@app.delete("/students/{student_id}")
def delete_student(student_id: int):

    if student_id not in students:

        raise HTTPException(
            status_code=404,
            detail="Student Not Found"
        )

    deleted_student = students.pop(student_id)

    return {
        "message":"Student Deleted",
        "student":deleted_student
    }
```

---

# Internal Flow

Before:

```python
{
   1:{
      "name":"Ravi"
   }
}
```

---

Execute:

```python
students.pop(1)
```

---

After:

```python
{}
```

Student removed.

---

# Complete Project Code

```python
from fastapi import FastAPI, HTTPException

app = FastAPI()

students = {}

# CREATE
@app.post("/students")
def create_student(student: dict):

    student_id = len(students) + 1

    student["id"] = student_id

    students[student_id] = student

    return {
        "message":"Student Created",
        "student":student
    }

# READ ALL
@app.get("/students")
def get_students():

    return {
        "students": list(students.values())
    }

# READ ONE
@app.get("/students/{student_id}")
def get_student(student_id: int):

    if student_id not in students:

        raise HTTPException(
            status_code=404,
            detail="Student Not Found"
        )

    return students[student_id]

# UPDATE
@app.put("/students/{student_id}")
def update_student(
    student_id: int,
    student: dict
):

    if student_id not in students:

        raise HTTPException(
            status_code=404,
            detail="Student Not Found"
        )

    student["id"] = student_id

    students[student_id] = student

    return {
        "message":"Student Updated",
        "student":student
    }

# DELETE
@app.delete("/students/{student_id}")
def delete_student(student_id: int):

    if student_id not in students:

        raise HTTPException(
            status_code=404,
            detail="Student Not Found"
        )

    deleted_student = students.pop(student_id)

    return {
        "message":"Student Deleted",
        "student":deleted_student
    }
```

---

# Project Workflow Diagram

```text
Client
  |
  | POST /students
  V
Create Student
  |
  V
Dictionary Storage

--------------------------------

Client
  |
  | GET /students
  V
Return All Students

--------------------------------

Client
  |
  | GET /students/1
  V
Return One Student

--------------------------------

Client
  |
  | PUT /students/1
  V
Replace Student Data

--------------------------------

Client
  |
  | DELETE /students/1
  V
Remove Student
```

---

# Limitations of In-Memory Storage

Suppose server restarts:

```bash
CTRL + C
```

Data becomes:

```python
students = {}
```

Everything lost.

Why?

Because data lives only in RAM.

---

# Why We Learn This First?

Because students understand:

```text
Routing
Path Parameters
Request Body
HTTP Methods
CRUD
HTTPException
Validation Basics
```

without database complexity.

---

# Industry Upgrade Path

Current:

```text
FastAPI
     ↓
Dictionary
```

Next Level:

```text
FastAPI
     ↓
Pydantic Models
     ↓
MongoDB / PostgreSQL
```

This CRUD project is the foundation before moving to real databases and Pydantic validation.
