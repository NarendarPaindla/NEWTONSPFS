# Building a Full CRUD API with In-Memory Storage (FastAPI)

This is usually the **first complete backend project** students build before learning databases.

---

# What Are We Building?

A Student Management API.

Features:

```text
Create Student
Get All Students
Get One Student
Update Student
Delete Student
```

Storage:

```text
Python Dictionary
```

No database yet.

---

# Final API Endpoints

| Method | Endpoint       | Purpose          |
| ------ | -------------- | ---------------- |
| POST   | /students      | Create Student   |
| GET    | /students      | Get All Students |
| GET    | /students/{id} | Get One Student  |
| PUT    | /students/{id} | Update Student   |
| DELETE | /students/{id} | Delete Student   |

---

# Step 1: Create Project

```text
crud_project/
|
├── main.py
```

---

# Step 2: Install FastAPI

```bash
pip install fastapi uvicorn
```

---

# Step 3: Create Basic Application

```python
from fastapi import FastAPI

app = FastAPI()
```

Run:

```bash
uvicorn main:app --reload
```

Visit:

```text
http://127.0.0.1:8000/docs
```

Swagger opens.

---

# Step 4: Create In-Memory Storage

Think of this as a fake database.

```python
students = {}
```

Current state:

```python
{}
```

No students.

---

# Why Dictionary?

Fast lookup.

```python
students[1]
```

gets student with ID 1 instantly.

---

# Step 5: Create Student (POST)

First feature.

Client sends:

```json
{
    "name":"Ravi",
    "age":21,
    "course":"CSE"
}
```

Server stores it.

---

## Code

```python
@app.post("/students")
def create_student(student: dict):

    student_id = len(students) + 1

    student["id"] = student_id

    students[student_id] = student

    return {
        "message":"Student Created",
        "student":student
    }
```

---

# Internal Flow

Initially:

```python
students = {}
```

---

Request:

```json
{
    "name":"Ravi",
    "age":21
}
```

---

After insertion:

```python
{
    1:{
        "id":1,
        "name":"Ravi",
        "age":21
    }
}
```

---

# Test in Swagger

POST

```text
/students
```

Body:

```json
{
  "name":"Ravi",
  "age":21,
  "course":"CSE"
}
```

---

Response:

```json
{
  "message":"Student Created",
  "student":{
      "id":1,
      "name":"Ravi",
      "age":21,
      "course":"CSE"
  }
}
```

---

# Step 6: Get All Students

Requirement:

```text
Show all students
```

---

## Code

```python
@app.get("/students")
def get_students():

    return {
        "students": list(students.values())
    }
```

---

# Why list()?

Dictionary:

```python
{
    1:{},
    2:{}
}
```

---

We only need:

```python
[
   {},
   {}
]
```

Student records.

---

Request:

```http
GET /students
```

---

Response:

```json
{
  "students":[
      {
          "id":1,
          "name":"Ravi"
      }
  ]
}
```

---

# Step 7: Get Single Student

Requirement:

```text
Get Student By ID
```

---

Request:

```http
GET /students/1
```

---

## Code

```python
from fastapi import HTTPException

@app.get("/students/{student_id}")
def get_student(student_id: int):

    if student_id not in students:

        raise HTTPException(
            status_code=404,
            detail="Student Not Found"
        )

    return students[student_id]
```

---

# Internal Flow

Request:

```http
/students/1
```

---

Check:

```python
if 1 not in students
```

False.

Return student.

---

Response:

```json
{
  "id":1,
  "name":"Ravi",
  "age":21
}
```

---

# Student Not Found Case

Request:

```http
/students/100
```

---

Response:

```json
{
  "detail":"Student Not Found"
}
```

Status:

```http
404 Not Found
```

---

# Step 8: Update Student (PUT)

Requirement:

```text
Replace student information
```

---

Current Student

```json
{
  "id":1,
  "name":"Ravi",
  "age":21
}
```

---

Request

```json
{
  "name":"Rahul",
  "age":25
}
```

---

Updated Record

```json
{
  "id":1,
  "name":"Rahul",
  "age":25
}
```

---

## Code

```python
@app.put("/students/{student_id}")
def update_student(
    student_id: int,
    student: dict
):

    if student_id not in students:

        raise HTTPException(
            status_code=404,
            detail="Student Not Found"
        )

    student["id"] = student_id

    students[student_id] = student

    return {
        "message":"Student Updated",
        "student":student
    }
```

---

# Test

Request:

```http
PUT /students/1
```

Body:

```json
{
  "name":"Rahul",
  "age":25,
  "course":"ECE"
}
```

---

Response:

```json
{
  "message":"Student Updated",
  "student":{
      "id":1,
      "name":"Rahul",
      "age":25,
      "course":"ECE"
  }
}
```

---

# Step 9: Delete Student

Requirement:

```text
Remove Student
```

---

Request:

```http
DELETE /students/1
```

---

## Code

```python
@app.delete("/students/{student_id}")
def delete_student(student_id: int):

    if student_id not in students:

        raise HTTPException(
            status_code=404,
            detail="Student Not Found"
        )

    deleted_student = students.pop(student_id)

    return {
        "message":"Student Deleted",
        "student":deleted_student
    }
```

---

# Internal Flow

Before:

```python
{
   1:{
      "name":"Ravi"
   }
}
```

---

Execute:

```python
students.pop(1)
```

---

After:

```python
{}
```

Student removed.

---

# Complete Project Code

```python
from fastapi import FastAPI, HTTPException

app = FastAPI()

students = {}

# CREATE
@app.post("/students")
def create_student(student: dict):

    student_id = len(students) + 1

    student["id"] = student_id

    students[student_id] = student

    return {
        "message":"Student Created",
        "student":student
    }

# READ ALL
@app.get("/students")
def get_students():

    return {
        "students": list(students.values())
    }

# READ ONE
@app.get("/students/{student_id}")
def get_student(student_id: int):

    if student_id not in students:

        raise HTTPException(
            status_code=404,
            detail="Student Not Found"
        )

    return students[student_id]

# UPDATE
@app.put("/students/{student_id}")
def update_student(
    student_id: int,
    student: dict
):

    if student_id not in students:

        raise HTTPException(
            status_code=404,
            detail="Student Not Found"
        )

    student["id"] = student_id

    students[student_id] = student

    return {
        "message":"Student Updated",
        "student":student
    }

# DELETE
@app.delete("/students/{student_id}")
def delete_student(student_id: int):

    if student_id not in students:

        raise HTTPException(
            status_code=404,
            detail="Student Not Found"
        )

    deleted_student = students.pop(student_id)

    return {
        "message":"Student Deleted",
        "student":deleted_student
    }
```

---

# Project Workflow Diagram

```text
Client
  |
  | POST /students
  V
Create Student
  |
  V
Dictionary Storage

--------------------------------

Client
  |
  | GET /students
  V
Return All Students

--------------------------------

Client
  |
  | GET /students/1
  V
Return One Student

--------------------------------

Client
  |
  | PUT /students/1
  V
Replace Student Data

--------------------------------

Client
  |
  | DELETE /students/1
  V
Remove Student
```

---

# Limitations of In-Memory Storage

Suppose server restarts:

```bash
CTRL + C
```

Data becomes:

```python
students = {}
```

Everything lost.

Why?

Because data lives only in RAM.

---

# Why We Learn This First?

Because students understand:

```text
Routing
Path Parameters
Request Body
HTTP Methods
CRUD
HTTPException
Validation Basics
```

without database complexity.

---

# Industry Upgrade Path

Current:

```text
FastAPI
     ↓
Dictionary
```

Next Level:

```text
FastAPI
     ↓
Pydantic Models
     ↓
MongoDB / PostgreSQL
```

This CRUD project is the foundation before moving to real databases and Pydantic validation.



# How to Test a FastAPI API Using URL and JSON

As a backend developer, you will test APIs using:

```text
1. Browser
2. Swagger UI
3. Postman
4. cURL
5. Frontend Application
```

Let's use this API:

```python
from fastapi import FastAPI

app = FastAPI()

@app.post("/students")
def create_student(student: dict):
    return {
        "message": "Student Created",
        "student": student
    }
```

---

# Method 1: Swagger UI (Recommended for Beginners)

Run:

```bash
uvicorn main:app --reload
```

Open:

```text
http://127.0.0.1:8000/docs
```

You will see:

```text
POST /students
```

Click:

```text
Try it Out
```

Enter JSON:

```json
{
  "name": "Ravi",
  "age": 21,
  "course": "CSE"
}
```

Click:

```text
Execute
```

---

Request Sent:

```http
POST /students
```

Body:

```json
{
  "name": "Ravi",
  "age": 21,
  "course": "CSE"
}
```

---

Response:

```json
{
  "message": "Student Created",
  "student": {
    "name": "Ravi",
    "age": 21,
    "course": "CSE"
  }
}
```

---

# Method 2: Postman

### Step 1

Open Postman

### Step 2

Select:

```text
POST
```

### Step 3

Enter URL:

```text
http://127.0.0.1:8000/students
```

---

### Step 4

Go to:

```text
Body
 ↓
raw
 ↓
JSON
```

Paste:

```json
{
  "name": "Ravi",
  "age": 21,
  "course": "CSE"
}
```

---

### Step 5

Click:

```text
Send
```

---

Response:

```json
{
  "message": "Student Created",
  "student": {
    "name": "Ravi",
    "age": 21,
    "course": "CSE"
  }
}
```

---

# Method 3: cURL

Very common in DevOps and Linux servers.

```bash
curl -X POST \
"http://127.0.0.1:8000/students" \
-H "Content-Type: application/json" \
-d '{
    "name":"Ravi",
    "age":21,
    "course":"CSE"
}'
```

---

Response:

```json
{
  "message":"Student Created",
  "student":{
    "name":"Ravi",
    "age":21,
    "course":"CSE"
  }
}
```

---

# Testing GET API

API:

```python
@app.get("/students")
def get_students():
    return ["Ravi", "Kiran"]
```

---

URL:

```text
http://127.0.0.1:8000/students
```

Browser works because GET requests don't need JSON.

---

Response:

```json
[
  "Ravi",
  "Kiran"
]
```

---

# Testing Path Parameters

API:

```python
@app.get("/students/{student_id}")
def get_student(student_id: int):
    return {
        "id": student_id
    }
```

---

URL:

```text
http://127.0.0.1:8000/students/101
```

---

Response:

```json
{
  "id": 101
}
```

---

# Testing Query Parameters

API:

```python
@app.get("/students")
def students(page: int):
    return {
        "page": page
    }
```

---

URL:

```text
http://127.0.0.1:8000/students?page=2
```

---

Response:

```json
{
  "page": 2
}
```

---

# Real Industry Testing Flow

For Student Creation API:

```http
POST /students
```

URL:

```text
http://127.0.0.1:8000/students
```

Body:

```json
{
  "name":"Ravi",
  "age":21,
  "course":"CSE"
}
```

Headers:

```http
Content-Type: application/json
```

---

# Understanding URL + JSON Together

Think:

```text
URL
 ↓
Which API?

JSON Body
 ↓
What Data?
```

Example:

```http
POST /students
```

asks:

```text
Which API?
```

Answer:

```text
Create Student API
```

Body:

```json
{
  "name":"Ravi",
  "age":21
}
```

asks:

```text
What Student?
```

Answer:

```text
Ravi, Age 21
```

---

# Interview Question

### How do you test a POST API?

1. Start FastAPI server
2. Open Swagger/Postman
3. Enter URL
4. Send JSON body
5. Verify response and status code

Example:

```http
POST http://127.0.0.1:8000/students
```

Body:

```json
{
  "name":"Ravi",
  "age":21
}
```

This is the exact workflow backend developers follow daily while building and testing APIs.

